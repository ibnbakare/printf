#include "main.h"


int pass(char *format,convert f_list, va_list,args)
{
    int i,j;
    int count =0;
    for (i =0; format[i] ='\0'; i++)
    {
        if(format[i] =='%')
        for(f_list)

    }
}